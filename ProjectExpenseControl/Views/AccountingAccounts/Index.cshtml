@model IEnumerable<ProjectExpenseControl.Models.AccountingAccount>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ACC_DES_ACCOUNT)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ACC_DES_ACCOUNT)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ACC_IDE_ACCOUNT }) |
            @Html.ActionLink("Details", "Details", new { id=item.ACC_IDE_ACCOUNT }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ACC_IDE_ACCOUNT })
        </td>
    </tr>
}

</table>
