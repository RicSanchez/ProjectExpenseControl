@model IEnumerable<ProjectExpenseControl.DataAccess.Role>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TUSR_DES_TYPE)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TUSR_FH_CREATED)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TUSR_DES_TYPE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TUSR_FH_CREATED)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TUSR_IDE_RESOURCE }) |
            @Html.ActionLink("Details", "Details", new { id=item.TUSR_IDE_RESOURCE }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TUSR_IDE_RESOURCE })
        </td>
    </tr>
}

</table>
